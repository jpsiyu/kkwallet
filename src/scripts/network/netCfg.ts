import { Net } from '@/scripts/network/netStruct.ts'

const available: Net[] = [
  new Net(1, 'Main Ethereum Network', 'red', ''),
  new Net(2, 'Ropsten Ethereum Network', 'yellow', ''),
]

export default {
  available
}
